var re = require('../lib/rendering');

exports['Should render html'] = function(test)
{
	var obj = {
	rows: [
			doc
			{
				key: 'value'
			}
		]
	};

	re.JsonToHtml(obj, function(row)
	{
		console.log(row);
	});
	
	test.done();
};
