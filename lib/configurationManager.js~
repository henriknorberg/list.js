var fs = require('fs');

var configManager = new function (path)
{
	this.cfg = {};
	this.ReadConfig = function()
	{
		//Reading configuration
		var cfg = fs.readFileSync(path);
	
		this.cfg = JSON.parse(cfg);
	};
};

exports.ConfigManager = configManager;
