{
  "_id" : "_design/lists",
  "language" : "javascript",
  "views" : {
    "lists" : {
      "map" : "function(doc){ if(doc.type == 'list') emit(doc._id, doc._rev)}"
    },
    "listnames" : {
    	"map" : "function(doc) { if(doc.type == 'list') emit(doc._id, doc.listname);}"
    }
  }
}

